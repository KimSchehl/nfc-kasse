# Kassensystem mit FastAPI

## Projektstruktur
- `app/` enthält die FastAPI-Anwendung
- `static/` enthält HTML und JS
- `routers/` enthält API-Endpunkte

## Start (im Dev-Modus)
```bash
./run.sh
```
Webseite: http://localhost:5000
